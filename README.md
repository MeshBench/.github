# .github

Organisation-level files for **MeshBench** — an RF-accurate MeshCore network
simulator.

`profile/README.md` is what appears on the
[organisation page](https://github.com/MeshBench), along with the banner beside
it. Both come from the brand repository and are generated rather than drawn, so
replace them by rebuilding there rather than by editing a PNG.
